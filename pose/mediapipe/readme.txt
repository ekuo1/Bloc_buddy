pip install mediapipe
python 3.10 and below

pip install --upgrade imutils

> when downloading & integrating code:
github recommends VS code
be sure to use python 3.10, and python 3.10 interpretor
set up venv--> activate it, and download all packages in venv
command to setup venv ctrl+shift+p, select venv

troubles with converting mediapipe 'NormalizedLandmark' object
online suggestion for protobuf_to_dict function doesn't load properly